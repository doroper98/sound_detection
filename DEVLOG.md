# SoundField — 개발 로그

## 추적 매트릭스

| Step | REQ | SC | EXP | 결과 |
|---|---|---|---|---|
| S1.1 공간/폰 UI | UI-001, SPACE-001, VIEW-001 | 01,02,05 | EXP-001 | PASS |
| S1.2 물리/관측 | AUDIO-001, DEVICE-001, LOC-001 | 03,04,06 | EXP-001 | PASS (합성 조건) |
| S1.3 내보내기/문서 | DATA-001, DOC-001 | 07,08 | EXP-001 | PASS (로컬) |
| S1.4 배포 | DEPLOY-001 | 09 | EXP-001 | PASS |
| S1.6 야외 수음 논의·가능성 문서 | DOC-002 | 19 | EXP-006 | PASS (문서·계산 검증, 야외 실측 아님) |
| S1.7 iPhone 17 Pro 첫 실기기 진단 기록 | LIVE-002 | 17 | EXP-007 | 결과 확보 (Chrome for iOS 모노, 4채널 미확인, Safari 앱 미검사) |

## EXP-001 · S1.1–S1.4 — 최초 가상 실험실

- 날짜: 2026-09-22
- 요구사항: GOAL.md의 REQ-UI/SPACE/AUDIO/DEVICE/VIEW/LOC/DATA/DOC/DEPLOY.
- 변경: src/, tests/, scripts/, public/, 설정·workflow·정규 문서.
- 원본 BP 참조: 58c946ed28d3e96b3a6e618c72b86d1c8f0d6fc8.
- 설계: 정답 음압과 관측 기반 추정을 분리. 하나의 마이크 방향 추정을 만들지 않음. 두 마이크 단일 관측의 다중 후보를 그대로 표시.
- 검증: TypeScript strict + Vite 빌드 PASS. 모델 테스트 9/9 PASS. Chromium E2E 2/2 PASS. 데스크톱/모바일 스크린샷 직접 확인.
- 미검증: 실제 하드웨어 수음, Safari 실기기, 실제 cm 정확도.
- 배포: 최초 OAuth 시간 초과 후 사용자 요청으로 브라우저를 열어 인증 완료. Cloudflare Workers 영구 URL https://soundfield-lab.monosound09.workers.dev 배포 성공. Quick Tunnel은 사용하지 않음.
- 원격 검증: 공개 사이트 HTTP 200, version.json 0.1.0, 보안 헤더 확인 및 원격 Chromium E2E 2/2 PASS.
- 커밋 예정: `v0.1.0: Launch SoundField virtual acoustic localization lab`.

## 버그·실패 기록

### BUG-001 — npm 사내 인증서 오류 (RESOLVED)

- 원인: Node 기본 인증서에 사내 체인이 없어 npm이 SELF_SIGNED_CERT_IN_CHAIN으로 재시도.
- 수정: 실행 셸에 NODE_USE_SYSTEM_CA=1 설정. 운영체제 신뢰 저장소 사용.
- 검증: 의존성 설치 성공, audit 취약점 0건. TLS 검증 유지.

### BUG-002 — 첫 빌드 구문/API 오류 (RESOLVED)

- PhoneView lookAt 괄호 누락, 최신 lucide의 GitHub 브랜드 아이콘 부재, Vite 8 chunk 설정 함수 타입 변경.
- 수정: 구문 보정, GitFork 아이콘, 함수 기반 chunk 설정.
- 검증: TypeScript와 프로덕션 빌드 통과.

### BUG-003 — 추정 likelihood 언더플로 가능성 (RESOLVED)

- 원인: 많은 관측·작은 σ에서 모든 지수 적합도가 0으로 반올림될 수 있음.
- 수정: 격자 순위/후보 필터는 비용 공간에서 계산. 열지도 표시만 exp를 사용.
- 검증: 한 관측 다중 후보와 여러 관측 수렴 테스트 통과.

### BUG-004 — Three.js 그림자 상수 제거 경고 (RESOLVED)

- PCFSoftShadowMap 제거 경고를 보고 지원되는 PCFShadowMap으로 수정.

## 개선 로그

### 빌드 10 FOA 실시간 입력·대역 방향 열지도·진단 보존 — 2026-09-23

EXP-023 · REQ-NATIVE-026/027 · SC-48/49.

- 후면 카메라의 기본 수음을 iOS 26 FOA AudioDataOutput과 별도 Stereo 출력으로 연결했다. 전면/측정 상세의 기존 스테레오 검사는 유지한다. FOA는 런타임 지원 여부, 실제 ACN/SN3D 4채널 레이아웃, PCM 형식·유한 값, 내장 입력과 호스트 시각을 확인한다. SDK 26 선택을 검증 스크립트에 추가했다.
- FOA 프레임은 크기에 독립적인 4096 표본 큐로 분석한다. 160–600/600–2000/2000–8000Hz 대역별 방향 성분, 입력 레벨, 대표 주파수, 산포를 계산한다. 전체 대역의 중간 방향을 음원으로 만들지 않는다. 렌더는 가까운 방향의 가장 강한 대역 표시를 우선하며 최대 3개 후보다. 일반 음원 분리 아님.
- 기존 6단계 크기 보정을 우회한다. `X 전방/Y 좌측/Z 상방 → 세로 카메라`는 기기에서 확인 전인 축 가정이며 화면/JSON에 명시한다. 방향을 카메라에 투영할 때만 임시 길이 2를 쓰고 실제 거리·세계 음원 점을 추정하지 않는다. 같은 위상 반사는 후보로 통과할 수 있어 검증된 정확도/확률로 표현하지 않는다.
- 카메라 자세 시각 대응, bounded 대기, 무음/클리핑/모호성/오래된 결과 제거, 세션 중단·권한 취소·백그라운드 해제를 연결했다. 합성 및 실기기 입력을 구분한다. FOA와 AVAudioEngine은 함께 시작하지 않는다.
- BUG-023 보완: 중지/새 시작 시 JSON 통계를 UserDefaults에 최대 5개 보관하고 이전 진단 공유를 추가했다. UUID·날짜·버전·상태를 분리한다. PCM·영상은 저장하지 않는다.
- 순수 Swift 방향/반례/버퍼 경계 검사 5개 및 FOA 화면/무음/재시작 보관 UI 검사 3개 추가. 로컬 웹 gate PASS(40/40). Mac의 컴파일·Swift·UI·IPA 검증은 진행 중이며 실기기 FOA 접근·카메라 병행·축 대응·물리 정확도는 미검증이다.
- 첫 코드 `6afab88`의 [Mac 검증](https://github.com/doroper98/sound_detection/actions/runs/35865340749)에서 Xcode 26.0.1/SDK 26.0, Swift 78/78·focused UI 6/6·Release IPA PASS. 캡처에서 중앙 라벨이 강도 색을 덮고 청록 외곽만 보이는 문제를 발견해 따뜻한 중심 영역을 넓히고 픽셀 검증을 추가했다. 이후 대역별 거부 수치·형식 오류 진단·이전 기록 접기/공유 동선도 보강했다. 최종 코드 검증은 별도다.

### 공개 4채널 FOA 경로 발견·방향 계산과 실패 반례 — 2026-09-23

EXP-022 · REQ-NATIVE-024/025 · SC-46/47.

- 사용자에게 이미 받은 실패 JSON을 재요청했고, 사용자가 “아까 줬잖아. 그거야.”라고 정정했다. 사용자에게 다른 자료를 잘못 보냈다고 전제하지 않는다. 실패 안내와 제공된 보고서의 idle/표본 0 불일치는 앱의 진단 보존 문제로 계속 추적한다. 실제 버튼 순서와 유실 시점은 불명이다. 추가 자료를 받을 때까지 분석을 멈추지 않는다.
- 이전 API 조사에서 iOS 26의 `AVCaptureAudioDataOutput.spatialAudioChannelLayoutTag`를 놓쳤다. Apple WWDC25 및 공식 예제에서 audio-only capture input의 `.firstOrderAmbisonics`, 4채널 FOA와 별도 2채널 Stereo 동시 출력, 실시간 delegate 버퍼 접근을 확인했다. 예제를 기기에서 실행했다는 의미는 아니다. 예제는 iPhone 16 Pro 이후 지원 기기를 안내하며 실제 지원은 런타임 조회 대상이다.
- 기존 2채널 `AVAudioEngine`은 FOA를 요청하지 않는다. 4채널은 원시 물리 마이크 4개가 아니라 W/Y/Z/X 공간 성분이다. 기존 두 채널 시간차에 임의 마이크 간격을 대입하는 대신 대역별 FOA 방향을 계산하는 경로가 생겼다. 외부 장비로 넘어가기 전에 이 공식 내장 경로부터 확인한다.
- 보정 기준 0.04dB/도(±25° 양끝 약 2dB)와 1m 이상 안내는 물리 근거가 부족하다. 12cm **가상 무지향성** 배열의 1m 반례는 약 0.879dB라 탈락한다. 기존 테스트의 강한 합성 응답은 실제 iPhone 응답을 검증하지 않는다. 이 기준 문제는 확인됐지만 실제 실패의 유일한 물리 원인이라고 단정하지 않는다.
- 새 `scripts/audit-foa-feasibility.mjs`: 이상적 ACN/SN3D 단일 음원 8방향, W 기준 20dB SNR에서 최대 각도 오차 약 0.285°. 서로 다른 주파수의 두 방향은 대역별 복원하나 전체 평균은 음원 없는 중앙을 가리킨다. 같은 위상의 강한 반사에서는 벡터 일관성 1인데 직접음 방향에서 54.46° 틀린다. **수학·실패 조건 검증이며 iPhone 정확도 증거가 아니다.**
- 결정: 6단계 크기 보정을 반복시키는 작업보다 FOA 실제 채널/레이아웃/시각과 카메라 축 확인을 우선한다. 단순 일관성 점수만으로 반사를 통과시키지 않는다. 실제 수음 구현·새 IPA 배포는 이번 분석에서 수행하지 않았다. [상세 및 공식 출처](docs_canonical/NATIVE_LOCALIZATION_FEASIBILITY.md#공개-4채널-foa-경로-재진단-exp-022).
- 이전 main 병합 `7a4bf46`의 Native iOS [35860374770](https://github.com/doroper98/sound_detection/actions/runs/35860374770) 완료 SUCCESS 확인. 이 CI는 기존 앱에 관한 것이며 새 FOA 실기기 확인이 아니다.
- 이번 검증: FOA 합성 분석 assertion PASS, `npm run gate`(빌드·타입·단위 40/40·릴리즈 메타데이터) PASS, `git diff --check` PASS. 앱/렌더러를 변경하지 않아 새 UI·IPA 검증을 수행하지 않았다.

### 빌드 9 실측 해석·위치 추정 가능성 감사·전체 이력 보존 — 2026-09-23

EXP-021 · REQ-DOC-003 / REQ-NATIVE-024 · SC-45/46.

- 사용자 요청: 크기 차이 실패 원인을 명백히 찾고 시간차·연속 이동으로 실제 되는 방법을 찾을 것. 먼저 실패/발견/개발/수정 이력을 지정 문서에 기록해 커밋·푸시·main 병합할 것. [통합 진단](docs_canonical/NATIVE_LOCALIZATION_FEASIBILITY.md)에 EXP-001~020 인덱스와 최신 분석을 연결했다. 기존 실패 기록은 보존한다.
- 최신 JSON은 audioDetails로 시작한 15초·147구간의 audio-only 세션이다. 시작 때 카메라 false, 프레임 0, 모든 공간 거부가 cameraStopped다. 보정 idle/step=0/표본=0으로 별도 인용된 6단계 실패의 값이 없다. Core Motion과 AR은 별도다. 선택 필드 보고서를 원본 전체와 구분했다.
- BUG-022 (OPEN): 크기/시차 두 모델 모두 실패했을 때 화면은 크기 모델의 이유만 보여 준다. 인용 문구의 조건은 크기 응답 기울기 절댓값 <0.04 dB/도다. 시차도 이미 평가하며 하나만 합격해도 보정 성공이다. 실제 시차 탈락 이유와 크기 차이가 작았던 물리 원인은 이번 자료에 없다.
- BUG-023 (OPEN): 측정 상세의 새 수음 시작이 NativeReport와 SpatialModel을 초기화한다. 실패 뒤 새로 측정하면 이전 기록이 소실된다. 코드 경로는 확인했지만 실제 버튼 순서는 미확인이다. 초기화는 기존 설계이며 이번에는 수정하지 않았다.
- 발견: 시뮬레이터는 GCC-PHAT/peak 보간, 네이티브는 정규화 절댓값 상관/정수 lag/고정 peak margin이다. 기존 기하 검사에는 가상 40cm 간격과 분석적 TDOA도 포함돼 iPhone 실측 증거가 아니다. Apple 내장 Stereo의 여러 마이크 모델링 설명을 공식 출처로 확인했다.
- 오프라인 비교: 기존 JS 엔진과 Swift 계산의 JS 전사에 같은 합성 PCM을 주었다. 12 seeds의 저주파 직접음은 현재 방식 0/12 후보, GCC 평균 지연 오차 0.017 sample. **강한 반사에서는 둘 다 약 4.65 sample 오차**. 잡음 없는 합성 실험으로 실제 기기 개선을 입증하지 않는다. 고정 처리 지연의 반례도 지연 검출과 방향 정보를 구분한다.
- 12cm 가상 배열·정확한 위치·단일 고정 음원·독립 자세 8개에서 격자 오차 약 5.2cm. 동일 자세 반복은 퍼짐 5.3m를 남겼다. 수 m의 이동 조건이며 실제 기기 성능이 아니다. 재현 스크립트와 결과 JSON을 보존했다.
- 후속 기준: 실패 실행의 단계별 응답 확보 → 주파수별 크기/위상·시차 관계의 독립 방향 평가 → 검증된 방향과 AR 이동 결합. 내장 입력이 목표 정확도에 못 미치면 공통 클록·알려진 배열의 외부 입력을 별도 검토한다. 성공·구매·구현을 확정하지 않았다.
- 직전 `92b8c50`의 Native iOS [35857827251](https://github.com/doroper98/sound_detection/actions/runs/35857827251)와 Verify [35857827175](https://github.com/doroper98/sound_detection/actions/runs/35857827175) 완료 SUCCESS를 확인했다. 이전 기록의 UI 대기 설정 보완 후속 결과다. 앱 코드는 전달된 `7a0b468`과 같고 IPA는 교체하지 않았다.
- 이번 변경은 문서·통계·오프라인 스크립트다. 제품/앱 코드·IPA·릴리즈 번호는 유지한다. `npm run gate`의 타입·빌드·40/40 단위 테스트·v0.4.0 메타데이터 PASS. 새 스크립트를 빌드한 엔진으로 다시 실행해 반례 결과와 보고서 수치를 확인했다. 변경/추가 11개 파일의 UTF-8·JSON·문서 상대 링크 79개를 확인하고 `git diff --check`를 통과했다. 보조 Python 검증의 기본 CP949 읽기는 실패해 UTF-8을 명시한 재실행으로 수정했다. 이번에는 앱/UI 변경이 없어 로컬 iOS/E2E를 반복하지 않았다.

### 라이다 후속 계획과 기본 기능 검증 순서 — 2026-09-23

EXP-018 · REQ-NATIVE-018/019 · SC-39/40.

- 사용자 결정: 라이다로 관측한 공간과 카메라·소리 열지도를 결합하고 결과를 내보내는 기능은 후속 계획으로 남긴다. 지금은 기본 기능의 정확한 구현이 우선이다.
- Apple ARKit scene reconstruction/scene depth와 RoomPlan export 문서를 확인하고, 현재 AR 카메라 코드에 mesh/depth 요청이 없음을 확인했다. 공간 정보가 음향 정확도를 자동 보장하지 않으며 보이는 표면에 음원을 강제 배치하지 않는다는 검증 가설을 기록했다.
- `LIDAR_SPATIAL_EXPORT_PLAN.md`에 범위·한계·출력 초안·재검토 시점을, `NATIVE_FIELD_VALIDATION.md`에 수음 안정성·보정 반복성·기준 위치 오차·열지도/주파수·잘못된 표시 억제 검증 순서를 추가했다. GOAL/REPO_MAP/TESTING에 연결했다.
- 현재 작업에 매일 오전 10시 진행 상태를 확인하는 리마인드를 등록했다. 기본 실기기 검증 완료 또는 사용자 다음 단계 전환 때 한 번 알리고, 변화가 없으면 조용히 종료한다. 자동 라이다 구현은 하지 않는다. 최초 등록은 대상 작업 지정 누락으로 거부됐고 `destination=thread`로 수정한 호출이 성공했으며 등록 카드를 확인했다.
- 이번 변경은 계획/검증 문서이며 앱·IPA·릴리즈 버전은 변경하지 않았다. 빌드 7의 실기기 음향 정확도는 계속 미검증이다. 로컬 `npm run gate`의 타입·프로덕션 빌드·40개 단위 테스트·릴리즈 무결성 PASS. 문서 연결 및 `git diff --check`를 확인했다. 화면/실행 코드 변경이 없어 iOS 빌드와 E2E를 반복하지 않았다.

### 빌드 7 보정 거부 실측·빌드 8 진단 보완 — 2026-09-23

EXP-019 · REQ-NATIVE-020/021 · SC-41/42.

- 사용자 보고: 약 5분 44초·3435개 구간 양쪽 활성, 지연 후보 3219개, 복제 0, 마지막 누락/타임라인 단절 각각 1, 최근 실제 파형 58fps. 회전 보정은 6단계·150개를 끝내고 거부됐다. 별도의 기존 `calibration.trials=[]`는 사용하지 않은 방향 비교 기능이며 회전 보정 미실행을 뜻하지 않는다. 중지 뒤 tracking=false도 AR 미동작 증거가 아니다.
- BUG-019: 모든 적합 거부가 같은 문구였고 회전 단계 통계/실패 조건을 내보내지 않아 원인 분석이 불가능했다. 기존 조건을 유지한 평가 결과에 단계별 요약·두 방법별 기울기/반복 오차/잔차/판정 기준을 추가한다. 원음·영상 없이 거부 뒤에도 공유할 수 있다. 원본 전체를 재구성하지 않고 사용자 JSON의 선택 필드와 해석임을 명시한 보고서를 저장했다.
- BUG-020: 사용자에게 스피커 각도 부호를 보고 반대로 폰을 돌리게 하던 안내를 실제 폰 회전 화살표/동작 문구로 바꾼다. 표본 개수만으로 먼저 꽉 차던 막대에 실제 수집 경과 시간을 반영한다. 새 안내/거부 화면의 합성 UI와 실패 원인별 순수 Swift 회귀를 추가한다.
- 빌드 7 docs 커밋의 CI `35805854850`은 native 검증/IPA 포장 후 GitHub artifact 생성 요청 시간 초과로 실패했다. 이미 전달한 `13864bd` 검증/IPA와 구분한다. 빌드 8은 별도 전체 CI로 확인한다.
- 검증: 코드 `53f884b`에서 [Native iOS](https://github.com/doroper98/sound_detection/actions/runs/35842079170) Swift 67/67·합성 iOS UI 25/25·Release 기기 빌드·IPA 포장 PASS. [웹 CI](https://github.com/doroper98/sound_detection/actions/runs/35842079382) 단위 40/40·E2E 16/16 PASS. 로컬 gate PASS. 로컬 웹 E2E는 15/16 통과하고 기존 시뮬레이터 시나리오 한 건이 120초 제한을 넘었다. 그 한 건만 재실행해도 시간 초과했으며 trace에는 선행 assertion 완료 후 full-page screenshot 호출이 남아 있다. 원인은 확정하지 않았고 로컬 전체 PASS로 기록하지 않는다. 웹 실행 코드는 이번 변경에서 수정하지 않았다.
- IPA 529,084바이트, SHA-256 `48079428a3a231de336d6f621e9a187ecd7c7ee131f17fe7718fefed9d686af6`. arm64 iPhoneOS·빌드 8·CRC·신규 DEBUG 인자 제외를 확인했다. 실패 안내 캡처는 전체 화면을 검토했고, 회전 안내 캡처는 안내/취소만 확인되는 부분 렌더로 별도 표시했다. 실제 보정 실패 원인의 확정과 정확도 검증은 미완료다.
- 사용자 가로 모드 제안: Apple의 stereo input orientation과 영상 방향 일치 요구를 확인했다. 현재 빌드 8은 세로 고정이다. 가로 UI·마이크·AR 좌표를 함께 맞춘 뒤 세로와 비교하는 후속 항목을 실기기 검증 문서에 추가했다. 세로 자세를 이번 실패 원인으로 확정하지 않는다.

<!-- improvements -->

### 빌드 7 열지도와 영역 주파수 — 2026-09-23

EXP-017 · REQ-NATIVE-016/017 · SC-37/38.
- 요구 누락 수정: 빌드 6은 방향 띠/위치 원만 제공했다. 실제 수음 열지도와 영역 안 주파수 표시는 이번 빌드에서 연결한다.
- 동일 PCM의 가운데 최대 4096개 샘플에 평균 제거/주기 Hann/FFT를 적용하고 좌우 power를 평균한다. 역상 상쇄 없이 −65~−15 dBFS 고정 색상 척도에 연결한다. 대표 peak 주변 3 bins가 분석대역 power의 12% 이상이면 Hz/kHz, 아니면 에너지 10~90% 대역을 표시한다. 현재 한 음원 입력의 통계이며 음원 분리·SPL 측정이 아니다.
- 위치 계산은 유지하며 표시만 부드러운 열섬으로 변경. 방향만 있으면 높이를 발명하지 않고 세로 열지도 유지. 위치 반경은 표시 최소/최대 크기를 적용한 강조 영역이며 음압장/실제 물체 크기가 아니다.
- 원음/영상 저장 없음. 현재 주파수·열지도는 중지/배경/오래된 신호/추정 실패 시 제거. 독립 60Hz 파형 큐는 유지한다.
- 검증: 코드 `13864bd`에서 [Native iOS](https://github.com/doroper98/sound_detection/actions/runs/35804076363) Swift 59/59·합성 UI 23/23·Release 기기 빌드·IPA 포장 PASS. [웹 CI](https://github.com/doroper98/sound_detection/actions/runs/35804076241) 단위 40/40·E2E 16/16 PASS. 로컬 gate·웹 E2E 및 실제 시뮬레이터 캡처 검토를 완료했다. 실기기 음향 위치·SPL 정확도는 미검증이다.
- 첫 Mac CI `35802264374`: Swift 59/59 및 Release 기기 빌드는 통과했지만 DEBUG 합성 입력의 긴 map 수식을 Xcode가 제한 시간 안에 type-check하지 못했다. 명시적인 Double 중간값/for 루프로 분리하고 후속 전체 검증을 실행한다. 1kHz 합성 입력의 실제 FFT bin은 996.09Hz이므로 UI 검사도 측정 표시인 `996 Hz`를 기대하도록 정정했다. 자동 검증 화면의 범례는 하단 조작부 안에 배치해 겹침을 피한다.
- `4d0b372` / CI `35802847820`은 Swift 59/59·UI 23/23·Release를 통과했지만 화면 검토에서 tone 캡처의 열섬이 없었다. DEBUG 위치 fixture가 tracker의 잠시 보류된 bearing으로 spectrum을 먼저 지운 뒤 독립 위치용 bearing만 다시 넣던 상태 불일치였다. 최종 후보와 spectrum을 함께 만들고 report를 한 번만 게시하도록 수정한다. 실제 생산 accept 경로는 이미 원자적 보고서 갱신을 사용한다. 합성 카메라 안내와 범례 겹침을 제거하고 범례 공간은 항상 확보해 표시 여부에 따른 하단 이동을 없앤다. AX 글씨뿐 아니라 실제 화면 픽셀의 열섬 색과 조작부를 검사한 뒤 스크린샷을 보존한다. 캡처/350ms stale 조건은 완화하지 않는다.

### 빌드 6 카메라 위 방향·공간 후보 — 2026-09-23

EXP-016 · REQ-NATIVE-012/013/014/015 · SC-33/34/35/36. 사용자가 빌드 5 검사를 생략하고 위치 표현까지 개발한 파일을 요청했다.

- 후면 영상은 ARKit 세계 추적 세션이 단독 소유한다. AR 오디오 캡처를 끄고 기존 AVAudioEngine 경로를 유지한다. 전면은 기존 영상 전용 세션이며 공간 추정을 하지 않는다. 명시적 시작/권한/취소/인터럽트/백그라운드 해제를 유지한다.
- 고정된 한 광대역 음원을 카메라 중앙에 정렬한 뒤 제자리에서 0/−25/+25°를 두 번 회전한다. 실제 AR 자세와 같은 시각의 음향 통계를 모아 경험적 레벨 차이/신호 지연 응답을 적합한다. 약한 신호·기기 이동·고도 변화·재현성 부족은 보정을 거부한다. 물리 마이크 간격과 정답 위치는 가정하지 않는다.
- 보정된 범위에서만 수평 방향 띠를 표시한다. 0.45초 연속 자료, 최신 신호, 소리의 대략적인 스펙트럼 일치, 입력 경로/시각 조건을 검사한다. 무음/복제/포화/오래된 값은 숨긴다.
- 수평 방향 관측은 3D 광선이 아니라 평면으로 결합한다. 서로 다른 위치/자세 8개 이상·30cm 이상 이동, 행렬 퇴화·각도 잔차·전방/거리 범위·계산상 민감도를 검사해 조건부 위치 후보/거리를 투영한다. 같은 자세 반복/제자리 회전/세로를 계속 유지한 이동에서 거리·높이를 만들어내지 않는다.
- 원음/영상/월드맵 파일은 저장하지 않는다. schemaVersion 6의 명시적 JSON 공유에는 상대 AR 좌표와 경험적 보정/추정 통계를 포함한다. 보정과 추정은 해당 한 소리·환경·세션에 한정한다. 실기기 음향 정확도는 미검증이며 자동 시험 성공으로 대체하지 않는다.
- 최종 자동 검증 PASS: 코드 `5facebb`에서 [Native iOS](https://github.com/doroper98/sound_detection/actions/runs/35795916043) Swift 53/53·합성 UI 21/21·Release 기기 빌드·IPA 포장 PASS. [웹 CI](https://github.com/doroper98/sound_detection/actions/runs/35795915981) 단위 40/40·E2E 16/16 PASS. 로컬 gate·웹 E2E와 최종 화면 검토도 완료했다. 빌드 6 IPA 486,175바이트, SHA-256 `edaa60bc934e83dbd519a95aa29e50348d2f430ca7807616815e27b07ef0205d`. ZIP CRC·실제 iPhoneOS arm64·빌드 번호·새 DEBUG 위치/방향 인자 제외를 확인했다. 검증 JSON: `docs/reports/2026-09-23-native-build6-verification.json`.
- 순수 Swift에서는 응답 부호·반복/스펙트럼/범위 거부, 시각 대응, 퇴화/3D 합성 점 복원을 검증하고 시뮬레이터에서는 보정 전/무음 보류·방향 표시·중지 해제를 검사한다.
- 중앙 정렬은 도움말을 닫고 실제 카메라 화면 중앙 +를 보면서 별도 버튼으로 확정한다. 보정 도중의 AR 세션 손실과 비교 검사 간 동시 실행을 차단하며, 명시적 지도 초기화/재보정은 과거 후보도 제거한다. Windows 로컬 gate 40/40·웹 E2E 16/16 PASS. 한쪽 스피커용 합성 잡음 HTML의 시작/중지와 화면을 음소거한 브라우저로 확인했다.
- 첫 Native iOS `35794109661`에서 Swift 53/53·UI 20/20·Release 빌드 PASS. 방향 화면과 도움말을 직접 검토했다. 점 투영 UI 검사와 카메라 중앙 정렬의 후속 수정본은 별도 검증한다. 전체 화면 영상과 겹침 화면의 safe-area 범위를 같게 하여 projectPoint 뷰포트/중앙 기준점이 일치하게 했고 실제 화면 중심의 접근성 좌표를 검사한다.
- `35794487968`은 Swift 53/53·Release·UI 20/21 PASS. 새 위치 후보 표시/백그라운드 제거도 통과했다. 기존 무음 오른쪽 파형 검사는 왼쪽 AX 조회 뒤 오른쪽 조회에 약 1.3초가 걸려 350ms stale 제거 시점의 "입력 없음"을 읽었다. 오른쪽도 "평탄"이라는 새 입력 상태를 기다리도록 변경한다. 앱의 stale 기준·파형 내용·실패 조건을 낮추지 않는다.
- 위치 화면 검토에서 DEBUG 후보용 합성 자세와 일반 방향용 고정 12° 입력이 섞여 띠와 원이 반대편에 보이는 검사 데이터 문제를 확인했다. 위치 fixture에서는 같은 마지막 자세/합성 음원의 각도로 경험적 특성을 만들어 방향도 계산한다. Release 추정 경로에 음원 좌표를 추가하지 않는다. 후보 위 긴 설명이 거리 문구와 겹치던 화면은 짧은 상단 표시로 정리했다.

### 빌드 4 실측·빌드 5 방향 비교와 파형 갱신 — 2026-09-22

EXP-015 · REQ-NATIVE-010/011 · SC-31/32.

- 사용자 빌드 4 JSON을 `2026-09-22-iphone17pro-native-build4-stereo.json`에 값 보존했다. 27초·259개 모두 좌우 활성, 48kHz/2채널, 복제·분석 누락·불연속 0, 시작 알림 모두 유지, 재시작 0. 후보 15/259=5.79%, 마지막 이력 19개는 모두 ambiguous다. 마지막 91.64°는 폰 자세 변화다. 별도 analysis JSON에 해석을 기록하며 이번 소리 종류는 미제공으로 남긴다. 사용자는 수음 정상과 파형 끊김을 보고했다.
- 빌드 4의 파형은 분석과 함께 약 100ms마다 갱신됐다. 빌드 5는 독립된 제한 큐에서 실제 버퍼를 최대 60개/초의 짧은 파형으로 줄이고 CADisplayLink가 표시한다. 4800/48kHz 버퍼당 실제 10ms 창 6개, 좌우 공통 배율, 표시 지연 기본 120ms. 가짜 중간 파형을 생성하지 않는다. 작은 그래프 뷰만 별도 관찰한다. 최대 24개 축소 프레임·350ms stale 제거·중지/공유/배경 해제, 실제 새 표시 횟수/최근 1초 속도/버린 표시 프레임을 기록한다. 분석 임계값과 오디오 경로 설정은 유지한다.
- 폰 고정·후면 입력·한 소리원 조건에서 왼쪽/정면/오른쪽 × 두 번, 각 3초 준비+5초 관측을 안내한다. 구간별 상태 분포, 후보 비율, 중앙값/산포, R−L 레벨 차이, 초기 기준 자세 변화를 저장한다. 중단된 구간은 이유와 부분 통계를 보존하고 같은 단계를 다시 진행한다. 12회 시도/구간당 600통계 상한이다.
- 최소 20구간/3.5초 수집, 후보 10개 이상·60% 이상, 자세 대응 90% 이상·5° 이내, 동일 샘플률, 산포 3 samples 이내를 비교 조건으로 사용한다. 반복 차이와 단계 간 분리는 최소 3 samples 및 2×최대 산포로 비교한다. 실험용 품질 기준이며 정확도 보장이 아니다. `acousticCalibrationVerified`와 위치 활성 플래그는 계속 false다.
- 최종 자동 검증 PASS: 코드 `37bce7f`에서 [Native iOS](https://github.com/doroper98/sound_detection/actions/runs/35744865905) Swift 42/42·합성 UI 18/18·Release 기기 빌드·IPA 포장 PASS. [웹 CI](https://github.com/doroper98/sound_detection/actions/runs/35744865907) 단위 40/40·E2E 16/16 PASS. 로컬 gate와 E2E 16/16도 통과했다.
- 전달: 빌드 5 IPA 348,143바이트, SHA-256 `8d5d650b558f85f2dab7b74b17abc4ca8d60a0bb737ff227bd7de501592d048d`. Windows에서 ZIP CRC·iPhoneOS arm64·빌드 번호·DEBUG 인자 제외·해시를 확인하고 최종 파형/비교 화면을 직접 검토했다. 실기기 빌드 5 fps·방향 교정은 사용자 확인 대상이다. 검증 결과는 `docs/reports/2026-09-23-native-build5-verification.json`에 보존했다.
- 첫 CI `35737686458`: Swift 42/42·Release 기기 빌드 PASS, UI 18개 중 두 assertion 실패. DSP 직후 표시를 즉시 요구한 기존 파형 검사는 독립 표시의 120ms 지연을 기다리도록 수정했다. 방향 비교의 동일값 판정 검사는 타이머 도착 시각을 버퍼 시작으로 쓴 합성 입력에서 구간 중첩이 생길 수 있어, 일정 샘플 시계/공통 run loop로 수정 후 재검증한다. 새 파형 속도 UI 검사는 첫 실행에서 통과했다. UI 실패 시에도 xcresult 첨부를 내보내도록 했고 이미 테스트가 찍은 화면을 사용해 별도 시뮬레이터 재부팅을 제거했다.
- 두 번째 CI `35738288752`의 화면과 접근성 기록에서 6구간 완료·후보 100%를 확인했으나 일부 구간은 실제 전달량 부족으로 보류했다. 합성 입력의 샘플 시계 수정만으로 해결됐다고 주장하지 않는다. 화면을 덮는 상세/비교 시트에서는 display link를 중지해 불필요한 뒤쪽 그리기를 제거했고, 복귀 시 최신 파형으로 재개한다. fps에는 측정 시각을 기록하고 상세에서는 마지막 표시 속도를 보여준다. 구간별 실제 수집 초도 표시한다. UI 검사는 CI 부하의 수집량 보류 또는 동일 시간차 보류가 모두 보수적 결과임을 허용하되 방향 성공은 금지한다. 충분한 자료의 정확한 동일값 판정은 순수 Swift 테스트가 별도로 강제한다. 시뮬레이터 접근성 조회 지연을 고려해 단계 완료 대기 상한은 20초로 늘렸다.
- 세 번째 검사 로그에서 파형 대기를 시작 버튼보다 앞에 삽입한 테스트 순서 오류를 확인했다. 앱이 대기 중인 정상 상태에서 수신을 기다려 실패한 것이므로 대기를 수음 시작 뒤로 옮겼다. 시작 전 빈 파형 검사는 유지한다. 중간 `35739306323`/`35740382916` 실행은 후속 수정본으로 대체하며 중지했다.
- `35741377971`: Swift 42/42·Release 빌드와 UI 17/18 PASS. 빠른 파형·상세 복귀·6구간 비교도 통과했다. 남은 실패는 구간 취소 검사에서 준비 조건으로 최근 0.5초의 안정된 +145.8µs를 요구한 것이었다. 수집 부하에서는 PCM이 들어와도 연속 추정이 보류될 수 있으므로, 시작/중지·카메라·비교 UI 검사는 실제 분석 개수가 0보다 큰 시점을 준비 조건으로 사용한다. 접근성 값으로 분석 개수를 제공한다. 시간차 숫자/안정성의 별도 Swift 검사와 상세 화면 검사는 유지한다.
- `35742815912`도 Swift 42/42·Release·UI 17/18로 완료했다. 남은 기존 알림 검사는 첫 조회가 아직 알림 0인 상태에서 접근성 snapshot 조회에 약 8초가 걸려 10초 대기 상한을 넘었다. 연속 분석/타이머의 보고서 필드별 publish를 한 번의 대입으로 묶어 화면 갱신 통지를 줄였다. UI의 정상 상태 수렴 대기는 30초로 확대하되 실제 신호·알림 값 assertion, 수음 timeout·추정 기준·권한 취소의 시간 조건은 유지했다.

### 좌우 실시간 미니 파형 — 2026-09-22

EXP-014 · REQ-NATIVE-009 · SC-30.

- 사용자 요청: 좌우에서 수음되는 소리를 작은 오실레이터로 표시. 실제 L/R 입력 파형을 보여주는 미니 오실로스코프로 구현했다.
- 카메라 화면 하단에 L(초록)/R(청록) 두 파형을 추가. 실제 분석 버퍼의 마지막 최대 10ms에서 채널당 최대 96개 min/max 열을 계산하고 두 채널에 공통 자동 배율을 적용한다. 순간 피크를 보존하고 한쪽이 무음이면 평탄하게 표시한다. 배율·시간 범위를 화면에 표시하며 파형 높이를 절대 음량으로 오인하지 않도록 기존 dBFS도 유지한다.
- 표시 자료는 보고서와 별도인 비-Codable 타입이며 최신 한 묶음만 메모리에 둔다. 중지/재시작/백그라운드/공유·오류 중지에서 제거하고 새 PCM 없이 350ms가 지나면 다음 100ms 갱신에서 지운다. 오디오 시작·경로 설정·분석 임계값은 변경하지 않았다.
- 검증 PASS: 코드 `d9b5c31`, [Native iOS 35733399591](https://github.com/doroper98/sound_detection/actions/runs/35733399591)에서 Swift 31/31·iPhone 16 Pro/iOS 18.5 UI 15/15·Xcode 16.4 Release 기기 빌드·IPA 포장 통과. 서로 다른 좌우 크기·피크/마지막 샘플 보존·여러 샘플률·무음/짧은 버퍼·잘못된 입력, Canvas의 양쪽/무음/지연/중지·백그라운드 표시를 확인했다. [Verify 35733399620](https://github.com/doroper98/sound_detection/actions/runs/35733399620)도 단위 40/40·Chromium E2E 16/16 PASS.
- 전달: 빌드 4 IPA 274,897바이트, SHA-256 `691f3a3cac129a026482a098657565cd599b82d953d36da33b0ede35dc864c2e`. Windows에서 해시·CRC·iOS arm64 실행 파일·빌드 번호·DEBUG 인자 제외를 확인했고 양쪽 파형 및 오른쪽 무음 화면을 직접 검토했다. `docs/reports/2026-09-22-native-waveform-verification.json`에 보존한다. 웹 버전 0.4.0 유지. 새 파형의 실제 아이폰 표시·성능은 사용자 확인 대상이다.

### 빌드 3 실제 2채널 수음 확인 — 2026-09-22

EXP-013 · REQ-NATIVE-001/002/004/005/007/008 · SC-22/23/25/26/28/29.

- 사용자 JSON `docs/reports/2026-09-22-iphone17pro-native-build3-stereo.json`을 값 보존·서식 정리해 저장했다. 사용자 추가 설명: 기침과 컴퓨터에서 재생한 영상 소리를 수음. 소리별 시각·위치·기기 배치는 기록되지 않았다. 별도 해석은 `2026-09-22-iphone17pro-native-build3-analysis.json`이다.
- 실제 입력: iOS 27.0, 후면 Stereo, 세션/하드웨어/tap 모두 2채널·48kHz. `cameraAndAudio`, 카메라 세션 시작 상태 true. 13:09:14–13:09:39 UTC 약 25초 동안 245개 분석 구간 모두 좌우 active. 복제 의심 0, 분석 건너뛰기 0, 분석 타임라인 불연속 0. 영상 전체 프레임 이력은 없어 카메라 연속성 검증과 구분한다.
- 시작 오류 회귀: 첫 PCM 전 category(3) 1회·override(4) 3회에서 모든 실제 입력 검사 항목이 true이고 continueCapture. 초기 엔진 재시작 0. 이번 관측에서는 빌드 2의 즉시 중지 오류가 재현되지 않았다.
- 시간차: 후보 14/245=5.714%. 마지막 약 2초 이력 20개 중 후보 1개(−83.33µs/−4 samples), 나머지 19개 ambiguous. 마지막 0.5초는 0/5 후보이고 최종 state는 stopped. 최신 peakCorrelation 0.87196이지만 peakMargin 0.001465는 현 코드의 0.08 기준 미만이라 보류했다. 높은 상관을 곧 유일한 지연으로 해석하지 않는다.
- 자세: 마지막 시작 대비 회전 약 25.36°, motion-minus-audio 약 −4.51ms를 기록했다. 소프트웨어 시각 대응이며 음원 방위·하드웨어 동기화 확인이 아니다.
- 제한: 원본은 최신 상세 1개/최근 약 2초 이력/전체 누적 개수다. 전체 245개의 상세나 후보 14개의 시각·소리 종류 대응을 복원하지 않는다. 스테레오 입력 확보와 위치 계산 가능성을 구분하고 교정/물리 마이크/동기화/위치 플래그는 false로 보존한다. 임계값을 낮추거나 이 한 관측만으로 알고리즘을 변경하지 않았다.
- 후속: 폰을 세로로 고정하고 한 위치의 소리를 왼쪽·정면·오른쪽에서 따로 내며 위치별 표시/JSON을 남겨 채널 시간차의 재현성을 비교한다. 이번 변경은 결과·검증 상태 문서화이며 앱 재빌드가 아니다.

### 빌드 2 실기기 override 중지 재현·빌드 3 수정 — 2026-09-22

EXP-012 · REQ-NATIVE-007/008 · SC-28/29.

- 사용자 원본: `docs/reports/2026-09-22-iphone17pro-native-build2-override.json`. iOS 27.0, 후면 Stereo 선택·세션/하드웨어/tap 2채널·48kHz·엔진 실행 상태였으나 첫 분석 전 reasonCode 4에서 중지했다. 분석 0은 무음 판정이나 스테레오 불가능 판정이 아니다.
- 결함: AVAudioSession.RouteChangeReason.override를 누락해 routeUnknown으로 분류했다. routeMatches도 검사 대상 이벤트를 short-circuit하여 false로 기록했기 때문에 실제 경로 불일치의 증거가 아니었다.
- 수정: override를 독립 이벤트로 분류하고 동일한 입력 경로·세션·PCM 형식을 직접 확인한다. 유지된 입력은 계속 수음하며 실제 불일치/외부 장치/인터럽트 중지와 초기 재시도 상한은 유지한다. 모든 이벤트에서 검사 항목과 당시 소스/방향/포맷·분석 프레임 수를 schemaVersion 4에 기록한다.
- 사용자 추가 설명은 카메라 시작 직후 함께 중지. 원본 cameraSessionRunningAtAudioStart=false와 차이가 있어 원본을 수정하거나 카메라 미실행으로 단정하지 않는다. 오디오 중지 콜백은 카메라도 해제한다. 시작 버튼 경로를 startControl로 추가 기록해 통합/상세 시작을 구분한다.
- 검증 PASS: 코드 `ac5562d`, [Native iOS 35730115675](https://github.com/doroper98/sound_detection/actions/runs/35730115675)에서 Swift 27/27·iPhone 16 Pro/iOS 18.5 합성 UI 13/13·Xcode 16.4 Release 기기 빌드·IPA 포장 통과. NotificationCenter reason 4를 첫 PCM 전에 게시해 카메라/수음 유지, 변경된 입력은 해제하는 두 회귀도 PASS. [Verify 35730115684](https://github.com/doroper98/sound_detection/actions/runs/35730115684) 단위 40/40·Chromium E2E 16/16 PASS.
- 전달 검사: 빌드 3 IPA 260,418바이트, SHA-256 `c4cf9e5e592f3c88dc68bf49ffd5a090ecbafdb1b371fe4b7fdb3f3cf81767a8`. Windows에서 해시·CRC·arm64 iOS 실행 파일·빌드 번호·카메라 권한·DEBUG 인자 제외를 검증하고 합성 화면을 직접 검토했다. `docs/reports/2026-09-22-native-override-verification.json`에 보존했다. 실제 iPhone의 빌드 3 수음 성공은 재설치 후 확인하며 자동 회귀 성공으로 대체하지 않는다.

### 실기기 시작 중지 수정·전체 화면 카메라 — 2026-09-22

EXP-011 · REQ-NATIVE-007/008 · SC-28/29.

- 사용자 보고: Windows Sideloadly에서 Done, 개발자 모드 재시작 후 앱 실행. 외부 마이크·블루투스 이어폰 없이 수음 시작 직후 경로/인터럽트 문구로 중지. 설치·실행은 사용자 확인이며 스테레오 수신 성공은 아님. 원본 JSON이 없어 발생한 알림 종류/사유는 아직 확정하지 않음.
- 확인한 결함: 모든 route/config/interruption 알림을 이유 확인 없이 중지로 처리. 시작 뒤 늦게 게시되는 정상 설정 알림과 interruption ended도 중지시켰고 다른 AVAudioEngine의 알림까지 수신했다.
- 수정: category/config 변경은 실제 내장 stereo 경로·방향·기존 2채널 PCM 형식·엔진 상태를 재검사. 정상 수음은 유지하고 첫 PCM 전 2초 이내에만 최대 2회 초기 엔진 재설정. 실제 장치 변경·interruption began·미디어 서비스 손실은 중지. 종료된 인터럽트만으로 자동 재개하지 않음. 해당 엔진만 관찰하고 최근 32개 알림 종류·원인 코드·판정·입력 통계를 schemaVersion 3에 보관.
- 카메라: 전체 화면 AVCaptureVideoPreviewLayer 위에 좌우 음량·연속 시간차와 통합 시작/중지 버튼. 기존 진단은 측정 상세 시트로 이동. 명시적 시작 → 카메라 권한/영상 세션 → 마이크 권한/수음 순서. 영상 입력만 사용하고 오디오 세션 자동 설정을 끄며 원음·영상 저장/전송이나 음원 위치 표시는 추가하지 않음.
- 수명: 카메라 권한 취소/거절, 중지, 백그라운드, 카메라 오류/인터럽트와 수음 중지 시 해제. DEBUG 카메라 검사는 실제 영상이 없는 합성 UI로 명시. 네이티브 빌드 번호 2로 교체 파일 식별, 웹 릴리즈 0.4.0 유지.
- 빠른 수음 시작 실패에서도 카메라를 해제하도록, SwiftUI가 중간 phase를 렌더링했는지에 의존하는 관찰 대신 명시적 중지 콜백을 연결했다. 카메라 성공 직후 모노 입력 거부 시 카메라 해제 회귀도 추가했다.
- 검증 PASS: 코드 `95cda61`, [Native iOS 35725200763](https://github.com/doroper98/sound_detection/actions/runs/35725200763)에서 Swift 25/25·iPhone 16 Pro/iOS 18.5 시뮬레이터 UI 11/11·Xcode 16.4 Release 기기 빌드·IPA 포장 통과. 지연된 설정 알림과 인터럽트 알림을 합성 게시하는 회귀, 카메라 화면·거절·취소·백그라운드·수음 실패 연동 해제를 포함한다. [Verify 35725200712](https://github.com/doroper98/sound_detection/actions/runs/35725200712)의 Gate/단위 40/40·Chromium E2E 16/16도 PASS.
- 전달 검사: 빌드 2 IPA 253,153바이트, SHA-256 `e27cfb0a630b4f277469ac97b0665dd01968e3973008af1ea0584bcfe3d094de`. Windows에서 해시·ZIP CRC·arm64 iOS 실행 파일·카메라 권한 plist·빌드 번호·DEBUG 실행 인자 제외를 재검사했고 합성 카메라 UI 화면을 직접 검토했다. `docs/reports/2026-09-22-native-camera-routefix-verification.json`에 결과 보존. 이전 파일과 구분한 `SoundFieldStereo-build2-unsigned.ipa`로 전달한다.
- 실기기 미검증: 새 빌드의 카메라/수음 동시 사용 및 원래 시작 오류 해결 여부는 재설치 후 확인해야 한다. 이전 앱의 사용자 설치·실행 확인을 새 빌드 또는 실제 스테레오 성공으로 기록하지 않는다.

### 연속 시간차 누적·기기 자세 기록·Windows 설치 준비 — 2026-09-22

EXP-010 · REQ-NATIVE-004/005/006 · SC-25/26/27.

- 사용자 지시: 스테레오를 실시간으로 계속 측정하고 이전 관측과 합쳐 계산하도록 개발 계속. 사용자는 Mac이 없고 회사 앱 링크를 통한 설치 경험만 있으며, 직접 사용할 배포 계정/인증서는 없음.
- 구현: 최근 2초/최대 240개 관측, 최근 0.5초 중앙값·MAD 산포·유효 비율·변화 그래프. 3개/150ms/60% 유효 기준, 새 관측 350ms 이상 없으면 100ms 주기 갱신에서 수치 보류. 최신 무음·복제·모호성은 과거 유효 숫자로 가리지 않음. 중지·재시작 시 누적 상태 분리.
- 모션: 명시적 수음 시작 후에만 Core Motion xArbitraryZVertical을 50Hz로 읽고 최대 4초/256개 보관. PCM 중간 host 시각에 가장 가까운 자세를 60ms 이내에서 대응, quaternion·시각 차·시작 대비 회전량을 schemaVersion 2 보고서에 기록. 하드웨어 동기화나 이동거리 측정으로 간주하지 않음.
- 범위: 연속 신호 지연의 누적 계산이며, 카메라/AR 위치 추적·교정된 음원 방위/거리·이동 드론의 위치 필터는 아직 연결하지 않음. 반복된 같은 배치를 독립 공간 관측으로 계산하지 않음.
- 배포 준비: Mac CI에서 검증한 Release/iphoneos .app을 unsigned IPA로 포장하고 CRC·플랫폼·실행 파일·SHA-256을 검증. Windows Sideloadly와 사용자 Apple 계정으로 개인 서명할 수 있도록 안내. 기업 링크 설치는 별도 Apple 배포 서명이 필요하며 설치 경험만으로 자격이 생기지 않음. 비밀번호·인증서 수집/외부 전송 없음.
- 검증: 로컬 웹 Gate 40/40 PASS. 신규 Swift 누적·시각·자세 테스트 10개와 기존 10개, iOS UI·IPA 포장은 Mac CI에서 검증 예정. 설치 성공·실기기 스테레오·정밀도와 구분해 후속 결과 기록.
- 첫 Mac CI `35720277567`: Swift 20/20 및 Release 기기 빌드 PASS, UI 4/5. 공유 검사에서 수음 중지 상태 검사는 통과했지만, 열린 네이티브 공유창을 앱 스크롤로 닫으려 하여 뒤쪽 시작 버튼의 hittable 검사가 실패했다. 공유창이 열린 시점에 이미 수음과 누적값이 중지되어야 하는 요구로 검사를 구체화해 공유창 존재·중지 버튼 상태·공유 중지 사유·누적값 보류를 함께 확인하도록 수정했다. 공유창 닫기 제스처 검증과 구분한다.
- 재검증 PASS: 코드 `9397574`, Native iOS `35721177466`에서 Swift 20/20·iPhone 16 Pro/iOS 18.5 UI 5/5·Xcode 16.4 Release 기기 빌드·unsigned IPA 포장 통과. Verify `35721177518`에서 Gate/단위 40/40·Chromium E2E 16/16 통과. 합성 연속 관측 화면을 직접 검토했다.
- 전달 검사: IPA 191,303바이트, SHA-256 `49b5fef58a2a0487e5d406511d04761b6061858752c1e34034f088f6ee840592`. Windows에서 ZIP CRC·해시·Info.plist·arm64 Mach-O iOS 플랫폼과 DEBUG fixture 인자 제외를 재확인했다. 영구 결과는 `docs/reports/2026-09-22-native-continuous-verification.json`; 개인 서명 설치와 실제 수음·회전 센서는 아직 미검증이다.

### iPhone 네이티브 스테레오 입력 — 2026-09-22

EXP-009 · REQ-NATIVE-001/002/003 · SC-22/23/24.

- 사용자 지시: 브라우저 모노 스펙트럼보다 위치 계산에 필요한 실제 스테레오 입력을 먼저 확보. 독립 2채널부터 검증하며 4채널을 선결 조건으로 두지 않음.
- 변경: SwiftUI 앱과 Xcode 프로젝트, AVAudioSession 내장 front/back stereo 선택, 실제 세션·노드·PCM 채널 검사, 레벨·무음·복제·포화·정규화 상호상관 지연 후보, 좌/정면/우 통계 표시와 JSON 공유.
- 제한: Apple의 처리된 스테레오를 물리 마이크의 독립 원음으로 단정하지 않음. 지연 부호는 right-minus-left이며 기존 SDK의 microphone0-minus1과 반대이므로 교정 없는 직접 연결을 하지 않음. physicalMicrophonesVerified/hardwareSynchronizationVerified/localizationEnabled는 false 유지.
- 수명: 명시적 시작, 늦은 권한 취소, 백그라운드, route/config/인터럽트, 5초 PCM 무응답, 공유 시 해제. 처리 대기열 한 개와 건너뛴 버퍼 수. 원음·영상·장치 ID 저장/전송 없음.
- 선택 근거: Apple 문서상 measurement는 primary microphone을 사용하므로 raw stereo 경로로 가정하지 않음. record/default/stereo/portrait로 고정. 카메라 병행과 가로 방향은 이 첫 입력 검증 범위에 포함하지 않음.
- 검증 상태: Windows 로컬 웹 Gate 및 Mac CI 검증 진행 중. Swift 합성 지연 테스트·iOS 빌드·시뮬레이터 UI와 실제 아이폰 검사를 구분해 최종 결과를 아래에 기록한다. 웹 버전 0.4.0 유지, 네이티브는 개발용 프로젝트이며 배포 릴리즈가 아님.
- BUG-015 (CI): 첫 Mac 실행에서 Swift 진단 테스트와 Release/iphoneos 빌드는 통과했으나, Debug 시뮬레이터 앱이 arm64+x86_64를 요청하고 로컬 Swift 패키지는 활성 arm64만 빌드하여 모듈 아키텍처 불일치로 UI 실행 전에 실패. Debug의 ONLY_ACTIVE_ARCH를 YES로 맞추어 시뮬레이터와 패키지를 동일 아키텍처로 빌드하도록 수정.
- 최종 검증: `b447dc1`의 Mac CI [35718003457](https://github.com/doroper98/sound_detection/actions/runs/35718003457) PASS. Xcode 16.4, Swift 진단 10/10, 서명 없는 Release/iphoneos 빌드, iPhone 16 Pro/iOS 18.5 시뮬레이터 UI 5/5. 합성 +7 sample 입력이 +145.8µs로 표시되는 스크린샷 직접 검토, docs/assets/native-stereo-synthetic.png에 보존. 소리 위치 기록·공유/중지·권한 대기 취소·모노 거부·백그라운드 동작 검증.
- 웹 회귀: 로컬 Gate 40/40·릴리즈 메타데이터, Chromium E2E 16/16 PASS. GitHub Verify [35718003413](https://github.com/doroper98/sound_detection/actions/runs/35718003413) PASS. 네이티브 추가로 웹 런타임/배포는 변경하지 않음.
- 미검증: iPhone 17 Pro 실제 내장 스테레오, 실제 권한 다이얼로그/오디오 인터럽트·경로 교체, 장시간 수음과 발열, 신호 처리 편향·물리 TDOA·위치 정확도. 시뮬레이터 결과를 실기기 결과로 해석하지 않음. 설치에는 Mac/Xcode와 사용자 Apple 계정의 기기 서명이 필요하며 서명 IPA/TestFlight는 생성하지 않음.

### v0.4.0 — 2026-09-22

| 변경 | 내용 |
|---|---|
| 릴리즈 | 모노 입력에서도 동작하는 실시간 주파수·음량 분석 |

EXP-008 · REQ-LIVE-002/003/004 · SC-17/20/21.

- 기존 main에는 Chrome 보고서만 있어 사용자 제공 Safari JSON을 이번 변경에 보존했다. 두 브라우저 모두 2채널 형식 중 CH 1만 활성. 이전 대화에 등장한 596a707 커밋은 원격 브랜치에서 확인되지 않았다.
- `/listen` 연속 PCM 수집, Hann FFT 스펙트럼·대역 RMS·채널 선택, 마지막 통계 내보내기와 기존 카메라 병행. 위치 SDK와 가상 음원 좌표는 사용하지 않는다.
- 권한 대기 취소/늦은 허용, Stop, pagehide/visibilitychange, 트랙 ended, processor error, 8초 무응답을 정리한다. 소리를 출력하거나 서버에 보내지 않는다.
- Gate 통과(타입·빌드·단위 40/40·v0.4.0 메타데이터), Chromium 전체 E2E 16/16 통과. 모바일 390×844/데스크톱 1366×768 화면을 직접 검토하고 숫자까지 같은 화면에 보이도록 실시간 패널 간격을 조정했다. 조정 후 연속 분석/화면 회귀 1/1 통과.
- 최초 로컬 E2E에서 기존 모노 진단 1개가 PCM timeout으로 실패했다. 이어진 실제 스트림 변환 및 4채널·복제·설정 무시·권한 오류·미지원 시나리오는 통과했다. 원인을 확정하지 않고 별도 회귀로 재확인한다.
- 위 timeout은 전체 E2E 재실행에서 재현되지 않았다(해당 시나리오 5.4초 통과). 제품의 timeout/장치 해제 처리는 유지하며 환경 문제로 단정하지 않는다.
- Cloudflare whoami에서 기존 토큰 만료/갱신 실패, 연결 가능한 브라우저에서도 로그인 필요 상태를 확인했다. GitHub 배포 secrets/environment도 없어 공개 배포는 미실행. 기존 공개 버전이 새 기능을 제공한다고 기록하지 않는다.

### iPhone 17 Pro 첫 실기기 진단 결과 기록 — 2026-09-22

EXP-007 · REQ-LIVE-002 · SC-17.

- 사용자가 실제 iPhone 17 Pro에서 공개 /diagnostics를 실행한 JSON 보고서를 전달. 원본을 docs/reports/2026-09-22-iphone17pro-crios-diagnostics.json에 보존하고 TESTING/LIVE_CAMERA_PLAN/GOAL/DEPLOYMENT/README/FIELD_ACOUSTICS_FEASIBILITY/REPO_MAP을 갱신.
- 환경: userAgent `iPhone OS 27_0_0`, `CriOS/153.0.8010.24`. 검사 절차가 요구한 Safari 앱이 아니라 Chrome for iOS(WebKit)다. iOS 버전 입력란은 비어 있었다. 카메라 켠 상태로 후면 1280×720 30 fps 확인.
- 결과: 4채널 exact·2채널 exact·기본 입력 모두 captured, 각 20 프레임 81,920 샘플 48 kHz. PCM 2채널이 도착했으나 채널 2는 세 요청 모두 peak 0. 채널 1 레벨 −54~−60 dBFS. 앱 판정 "4채널 안정 수신 미확인".
- 해석: `getSupportedConstraints()`에 channelCount가 없어 exact 요청은 거절이 아니라 무시됐다. 실효 입력은 모노이며 방향 추정에 필요한 다채널 입력은 이 경로에서 확보되지 않았다. 채널 2의 0이 브라우저 변환인지 캡처 형식인지는 판정 불가. 물리 마이크 수·독립성·동기화 판정으로 쓰지 않는다.
- 미검증: Safari 앱 결과, 카메라 끈 상태, 권한 거절·회전·잠금 복귀. 한 기기·한 브라우저·한 회의 결과로 다른 iOS/브라우저에 일반화하지 않는다.
- 범위: 문서와 보고서 보존이며 제품 버전은 v0.3.0 유지. 코드·UI 변경 없음.
- 검증: JSON 파싱과 프레임×4096=샘플 수 일치 확인. npm run gate PASS: 빌드·타입 검사·단위 테스트 30/30·v0.3.0 릴리즈 메타데이터. git diff --check PASS. UI/런타임 변경 없음.

### 야외 수음 가능성과 사용자 논의 기록 — 2026-09-22

EXP-006 · REQ-DOC-002 · SC-19.

- 사용자 목표: 밤하늘의 약 500m 거리 드론을 탐색하고 거리를 약 ±50m로 추정. 주파수별 지도, 원거리 측정, 이동 단일 마이크의 합성개구 가능성과 이전 카메라/Safari 논의를 저장소에 보존 요청.
- 변경: FIELD_ACOUSTICS_FEASIBILITY.md에 질문·결정·구현 상태, 개별 센서/배열/관측소 간격 구분, 조건부 계산식과 재현 코드, 1차 연구 출처, 검증 순서 기록. README/GOAL/파일 지도/카메라 계획에서 연결.
- 계산: 각 관측소의 방향 오차를 ±0.5° 이내로 가정한 대칭 2D 기하에서 100m 기선은 500m 표적에 459.5~548.3m 범위. 실측 하드웨어 정확도·보편적 최소 간격·3D 전체 오차 보장으로 해석하지 않음.
- 검토: 이동 단일 마이크 연구의 정지 조화 음원/회전 센서 조건과 비행 드론의 차이, 음향 전파 약 1.46초, 탐지 가능성과 거리 정확도 구분. 주파수로 음원 종류를 단정하지 않음.
- 이전 공개 사이트 native 캡처 점검의 로컬 JSON을 확인해 TESTING.md에 조건과 한계를 보존. 가상 장치 Chromium 검증과 iPhone 실기기 확인을 분리하고 외출 전 체크리스트 추가.
- 범위: 문서 보완이며 제품 버전은 v0.3.0 유지. 야외 탐지/거리 추정/주파수 지도 구현 또는 실측 성능 달성으로 기록하지 않음.
- 검증: 문서 내 JavaScript를 실행해 표의 3개 결과 재현 PASS. 변경 문서 7개의 UTF-8/상대 파일 링크 및 git diff --check PASS. npm run gate PASS: 빌드·타입 검사·단위 테스트 30/30·v0.3.0 릴리즈 메타데이터. UI/런타임 변경 없음.

### v0.3.0 — 2026-09-22

| 변경 | 내용 |
|---|---|
| 릴리즈 | 작은 슬라이더 설정과 실제 카메라·Safari 채널 진단 |

EXP-005 · REQ-LIVE-001/002, REQ-UI-003, NFR-001 · SC-16/17/18.

- 변경: /diagnostics 실제 카메라·PCM 수음 진단, 입력 채널 보존 Worklet, 원음 없는 보고서, 명시적 시작·해제와 취소 후 늦은 권한 결과 정리.
- 시뮬레이터와 진단은 별도 lazy 경로. 위치 SDK 변경 없음. 실제 진단은 source/scene 입력을 사용하지 않으며 방향 열지도를 그리지 않음.
- 설정 패널 데스크톱 225→112px, 모바일 280→174px. 탭별 슬라이더·프리셋만 표시하고 3D 영역 확대. 폰 렌더 크기는 컨테이너 여유 높이를 사용해 짧은 화면에서 버튼이 잘리지 않도록 수정.
- BUG-011 (검증): 합성 MediaStream 1/4채널이 Chromium의 MediaStreamAudioSource 경로에서 모두 2채널로 도착. 생성 후 channelCount 변경 대신 생성자 설정도 같은 결과. Chromium 처리 소스와 실제 브라우저 테스트로 내부 stereo 변환 확인. 실제 전달값을 그대로 표시하고 설정값을 검증값으로 쓰지 않음. 4채널 UI 검증은 native Web Audio 합성 소스를 해당 경계에 주입한다고 명시; 별도 테스트에서 실제 스트림 변환을 검증.
- BUG-012 (검증): Windows용 Playwright WebKit에서 캡처 API가 없어 합성 수음 시나리오 5개 실패. 이는 iOS Safari 하드웨어 결과가 아님. 미지원 안내·환경 보고서 저장은 WebKit 1/1 PASS. 미지원 오류도 JSON으로 내보낼 수 있도록 보완.
- BUG-013: 설정을 줄여도 기존 휴대폰 높이 공식 때문에 1366×768에서 하단 버튼이 잘림. 실제 scanner 컨테이너 높이 기준으로 폰 크기 계산. 장식용 상단 설명도 접어 3D 공간 확보.
- 검증: Gate/단위 테스트 30/30, Chromium E2E 13/13 PASS. 1366×768 설정 탭 모두 112px, 390×844에서 174px 및 내부/페이지 넘침 없음. 두 크기에서 3D canvas 높이 360px 초과 유지. 데스크톱·모바일·진단 화면 직접 검토, 최신 README 화면 갱신.
- 미검증: 실제 iPhone 17 Pro Safari 채널 수, 물리 마이크 독립성·동기화, 실측 위치 정확도. 자동 테스트를 실기기 성공으로 기록하지 않음.
- 배포: Pages deployment 8bcdddd5, https://soundfield-lab.pages.dev/diagnostics HTTP 200, version.json 0.3.0 및 동일 origin 카메라/마이크 권한 정책 확인. 추가 로그인 없이 배포 성공.
- 원격 검증: 공개 Pages 주소에서 Chromium E2E 13/13 PASS. 실제 미디어 권한은 자동 테스트의 합성 입력으로 대체했으며 iPhone 결과로 간주하지 않음.

### 문서 보완 — 2026-09-22

EXP-004 · REQ-DOC-001 · SC-08.

- 사용자 요청: 직전 배포 결과와 실제 카메라 웹앱 안내 문구까지 저장소에 커밋·푸시·main 병합.
- docs/updates/2026-09-22-v0.2.1.md에 전달한 안내문 전체를 보존하고 README에 링크 추가.
- 공개 URL, 데시벨 열지도 수정, 독립 엔진, 검증 결과, 후면 카메라/수음 채널 확인 순서와 현재 가상 실험이라는 범위를 기록.
- 문서 보완으로 제품 버전은 v0.2.1 유지. npm run gate PASS: 빌드, 단위 테스트 25개, 버전 문서 일치 확인.

### v0.2.1 — 2026-09-22

| 변경 | 내용 |
|---|---|
| 릴리즈 | 데시벨에 반응하는 열지도와 계정명 없는 공개 주소 |

EXP-003 · REQ-VIEW-003 / REQ-DEPLOY-002 · SC-15 / SC-14.

- BUG-007: PhoneView가 정규화된 방향 적합도/고정 Gaussian만 그려 수신 음량을 반영하지 않았음. PCM RMS를 별도로 전달하고 고정 척도에서 색·alpha·표시 임계값에 반영. 위치 추정 코드는 변경하지 않음.
- BUG-008: 시뮬레이터의 amplitude 상한이 큰 볼륨 입력을 같은 PCM으로 만듦. 고정 이득에 여유를 두고 실제 샘플만 ±1에서 포화, 포화 표시 추가.
- 변경: src/heatmap.ts, simulation.ts, PhoneView/App, 모바일에서도 보이는 수신 레벨과 tests/heatmap.test.ts/E2E.
- 검증: Gate 및 단위 테스트 25개 PASS. 두 신호의 40~100 dB 단계별 PCM 10 dB 증가와 지연 불변 PASS. 데스크톱/모바일 E2E 4개 PASS. 정답 비교 50/90 dB 및 모바일 설정 화면을 직접 검토하고 docs/assets에 보관. 원격 배포 검증은 아래에 기록.
- 첫 모바일 회귀 테스트가 열린 설정 패널에서 숨겨진 모드 버튼을 클릭해 시간 초과. 실제 사용자 순서에 맞게 패널 닫기 → 모드 선택 → 설정 열기로 수정.
- Pages 추가 OAuth 인증 성공. Wrangler 4.136의 기본 Pages 생성 명령이 Workers로 자동 위임되어 이전 주소에 v0.2.0을 배포하는 동작을 확인. 사용자의 계정명 없는 URL 요구에 따라 `pages project create --force`로 Pages 프로젝트를 명시적으로 생성. 기존 프로젝트 배포에는 force가 필요하지 않음.
- 실제 카메라 웹앱 문의에 대해 HTTPS/권한/후면 video, PCM 채널 진단, 독립 수음 검증, 카메라 축 교정을 단계별로 docs_canonical/LIVE_CAMERA_PLAN.md에 기록. 실제 촬영·수음은 이번 수정에 포함하지 않음.
- BUG-009: Pages 배포가 `--config wrangler.pages.jsonc`를 거부. Pages는 사용자 정의 설정 경로를 지원하지 않아 표준 wrangler.jsonc에 pages_build_output_dir를 설정하고 npm/CI 명령의 --config 제거.
- 배포: https://soundfield-lab.pages.dev, deployment 7efc0de4. HTTP 200/version.json 0.2.1/보안 헤더 확인 및 원격 Chromium E2E 4/4 PASS. 페이지에 계정명이 없는 URL 요구 완료.
- BUG-010 (검증): v0.2.1 Release workflow는 PASS했지만 별도 Verify에서 볼륨 테스트가 실패. 모드/입력 변경 직후 이전 canvas 면적 1354를 기준으로 잡아, 복귀 후 새 모드의 50 dB 면적 462와 비교했음. canvas 읽기 전에 렌더 프레임 완료를 기다리도록 테스트를 수정. 제품 코드·배포·태그는 그대로 유지하고 테스트 보완 커밋으로 관리.
- BUG-010 재검증: 데스크톱/모바일 볼륨 시나리오를 각 3회 반복하여 6/6 PASS, Gate 25개 단위 테스트 및 버전 무결성 PASS.

### v0.2.0 — 2026-09-22

| 변경 | 내용 |
|---|---|
| 릴리즈 | 3D 화면과 독립된 PCM 음향 엔진 및 Pages 주소 |

EXP-002 · REQ-ENGINE-001 / REQ-ARRAY-001 / REQ-UI-002 / REQ-VIEW-002 / REQ-DEPLOY-002.

- 엔진을 packages/localization으로 분리. DOM 없는 TypeScript 빌드. PCM → GCC-PHAT/순음 위상 → 위치 후보.
- 합성 신호 생성은 src/simulation.ts, 렌더링은 src/components에 한정. 정답 시간차를 바로 공급하는 코드는 테스트 fixture에만 남김.
- JSON engineInput/engineOptions와 CLI replay, npm 패키지 구성 추가.
- 좌우/세로 배열, 영상 위 방향 열지도, 데스크톱 viewport 고정 및 모바일 탭/하단 패널.
- 21개 단위 테스트, 데스크톱/모바일 E2E 2개 PASS. 실제 하드웨어/Safari 미검증.
- DOM 없는 엔진 빌드와 별도 consumer 설치/import PASS. CLI replay의 정답 좌표를 바꿔도 출력 동일, 6 sample 지연 복원 PASS. 패키지 약 6.7 kB, JavaScript/타입 선언/예제 포함.
- BUG-005: 숨긴 모바일 탭의 ResizeObserver가 0×0을 보고해 camera aspect NaN이 발생. 0 크기 resize를 건너뛰고 탭 전환 후 열지도/console error 회귀 검증 추가.
- BUG-006: CSS로 숨긴 모바일 가이드 텍스트의 접근성 이름 누락. aria-label 추가 후 가이드 테스트 통과.
- Git push의 기존 PAT에 workflow scope가 없어 거부됨. 기존 연결된 GitHub 앱으로 workflow만 작성하고 나머지는 git으로 전송하여 권한 범위에 맞게 게시. 별도 사용자 인증 불필요.
- Pages API는 기존 Workers OAuth의 pages:write 부족으로 code 10000 거부. 여러 로그인 창이 응답 없이 만료되어 추가 인증을 기다림. 계정 전체 workers.dev 이름은 다른 사이트에 영향을 주므로 변경하지 않음.
- v0.1.0 GitHub Verify/Release workflow 모두 PASS 확인. 기존 기록 보존.

### v0.1.0 — 2026-09-22

| 항목 | 변경 파일 | 내용 |
|---|---|---|
| 3D 공간 | room, SpatialView | 실내 모델, 물체/바닥 raycast, 파동, 수신기 표시 |
| 휴대폰 | PhoneView, styles | 시야 회전, 표면 열지도, 음압/적합도 구분 |
| 음향 | acoustics | SPL·파장·시간차·순음 모호성·격자 추정 |
| 실험 제어 | App, useAudio | 장치·마이크·좌표·관측·내보내기·미리듣기 |
| 검증 | tests, scripts | 단위/E2E, 버전 문서 검사 |
| 문서/릴리즈 | docs, workflows | BP 추적 체계, GitHub 릴리즈와 Cloudflare 배포 설정 |

## 교훈

1. ‘스피커로 수음’ 요청은 실제 센서인 마이크로 구체화하고 실제 수음과 가상 실험을 구분한다.
2. 두 마이크의 한 시간차에서 정답 점만 빨갛게 그리면 추정 정확도를 오해하게 만든다. 정답 음압과 후보 적합도를 분리한다.
3. 관측 반복만으로 독립 정보가 늘지 않는다. 중복 pose를 거부하고 위치·높이 변경을 안내한다.
4. 소프트웨어 테스트 수렴을 실제 스마트폰 성능으로 설명하지 않는다.
5. 브라우저 서체는 같은 사이트에서 제공해 오프라인 캐시 및 CSP와 일치시킨다.
6. 계정 인증 없는 준비 상태를 공개 배포 완료로 기록하지 않는다.

### 빌드 8 첫 보정 정지 · 빌드 9 시각 대응 보완 — 2026-09-23

EXP-020 · REQ-NATIVE-022/023 · SC-43/44.

- 사용자 JSON: 672개 구간 모두 좌우 활성·지연 후보, 48kHz·2채널, 누락 0. 공간 거부 644, 보정 단계 0/표본 0, currentDegrees 없음. `trackingAvailable=false`는 중지 후 상태라 실시간 추적 실패의 단독 증거가 아니다. Core Motion 자세와 AR 기기 위치는 별도 경로다.
- BUG-021: AR 대응이 nil이거나 오디오 시각이 유효하지 않으면 원인 기록 없이 보정 앞에서 반환하고, 큰 안내는 ‘폰을 잠시 멈춰 주세요’에 남았다. 중지하면 진행 중 원인까지 일반 취소 문구로 덮었다. 이 정지 표시/진단 누락을 수정한다. 실제 누락의 하위 원인은 빌드 8 로그로 확정 불가.
- 180ms 이내 지연 AR 프레임을 최대 4개 특징 요약 큐로 재대응한다. 원래 PCM midpoint, ±60ms 대응, 300ms 음향 신선도, 200ms 카메라 신선도, 2.5cm/3° 버퍼 내 이동 제한을 유지한다. 최신 자세를 오래된 소리에 임의 대입하거나 수락 임계값을 낮추지 않는다.
- ARSession.currentFrame에서 이미 도착한 실제 프레임을 확인하며, delegate와 중복/역순 프레임은 역사에 중복 적재하지 않는다. 미리보기 재구성이 공유 ARSession을 임의 pause하지 않게 세션 소유권을 컨트롤러로 한정한다. 이것을 관측된 기기 정지의 확정 원인으로 주장하지 않는다.
- schema 9에 synchronization(수신/연결/지연 회복/사유별 거부/마지막 검사)과 calibrationBeforeStop을 보존한다. 자세가 없을 때도 시간 초과가 작동한다. 소리/자세 조건으로 보류될 때 큰 안내가 원인을 우선한다. 별도 검사는 ‘입력 비교’로 이름을 분리한다.
- 검증: 로컬 npm run gate(40개 단위 테스트 포함) PASS. 새 순수 Swift 6개와 실제 큐 경로를 쓰는 합성 UI 2개를 추가했다. Mac CI 및 웹 E2E 결과 대기. 실기기 보정/위치 정확도는 미검증.

빌드 9 첫 CI 보완: 코드 cbd5b59의 Native iOS 35854761145에서 Swift 73/73·Release 기기 빌드 및 지연 프레임 첫 단계 통과는 성공했지만, UI 27개 중 카메라 누락 안내 검사 한 개에서 2개 assertion이 실패했다. 대기 중 음향 신선도 제한을 넘으면 cameraStale 원인이 audioTooOld로 덮여 일반 안내가 나온 것이 실제 캡처로 확인됐다. 만료를 계속 거부하면서 `waitingForCameraIssue`에 원래 대기 원인을 보존하고 큰 안내에 표시한다. 같은 원인의 중복 문구도 제거한다. 수정 후 순수 Swift·Release·핵심 UI 6개를 수동 focused 경로로 우선 검증하고 전체 회귀는 PR 자동 실행에서 유지한다. 이전 27/27 통과로 기록하지 않는다.

빌드 9 전달 검증: 코드 `7a0b468`의 [Native iOS](https://github.com/doroper98/sound_detection/actions/runs/35856433903)에서 Swift 73/73, 합성 UI 27/27, Release 기기 빌드·IPA 포장 PASS. [웹 CI](https://github.com/doroper98/sound_detection/actions/runs/35856433756)의 단위 40/40·E2E 16/16 PASS. 로컬 gate 및 직전 앱 코드의 로컬 웹 E2E 16/16 PASS(웹 코드는 동일). 캡처 2개를 직접 확인하고 IPA CRC/arm64 iPhoneOS/빌드 9/SHA-256/Release의 합성 플래그 제외를 확인했다. 별도 focused 실행 35856434110은 보정 회귀 2개를 포함해 5/6 통과했고 기존 파형 UI 5초 대기에서 시간 초과했다. 전체 실행은 같은 앱 코드에서 통과했다. 이후 UI 검사만 양쪽 순차 30초 대기로 보완했으며 앱의 350ms 만료/수음/위치 코드는 변경하지 않았다. 보완한 검사 대기 설정의 CI는 별도다. 실기기 정지 해결과 음향 위치 정확도는 미검증. [검증 보고서](docs/reports/2026-09-23-native-build9-verification.json).
